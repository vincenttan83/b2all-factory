import { DynamicDivDirective } from './dynamic-div.directive';

describe('DynamicDivDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDivDirective();
    expect(directive).toBeTruthy();
  });
});
